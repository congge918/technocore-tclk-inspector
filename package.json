{
  "name": "technocore-tclk-inspector",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild ./src/app.mjs --bundle --format=esm --outfile=./dist/app.js --minify",
    "test": "node --test",
    "check": "node scripts/check.mjs"
  },
  "license": "MIT",
  "dependencies": {
    "@noble/curves": "2.4.0",
    "@noble/hashes": "2.4.0",
    "@scure/base": "2.4.0"
  },
  "devDependencies": {
    "esbuild": "0.25.9"
  }
}
